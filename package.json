{
	"name": "autcypress",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"bs:master": "browserstack-cypress --cf ./browserstack.json run --sync --spec cypress/e2e/*.js"
	},
	"author": "",
	"license": "ISC",
	"devDependencies": {
		"@bahmutov/cypress-esbuild-preprocessor": "^2.2.0",
		"cypress": "^12.17.3",
		"esbuild": "^0.17.12",
		"eslint-plugin-cypress": "^2.14.0"
	}
}
